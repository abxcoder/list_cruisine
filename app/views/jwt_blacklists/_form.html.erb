<%= form_with(model: jwt_blacklist) do |form| %>
  <% if jwt_blacklist.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(jwt_blacklist.errors.count, "error") %> prohibited this jwt_blacklist from being saved:</h2>

      <ul>
        <% jwt_blacklist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :jwt, style: "display: block" %>
    <%= form.text_field :jwt %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
