<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo03" aria-controls="navbarTogglerDemo03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <%# <a class="navbar-brand" href="pages/dashboard">Food Hunter</a> %>
        <%# <%= link_to "Food Hunter", pages_dashboard_path, class:"nav-linnk" %>
        <%= link_to 'Food Hunter', root_path, class:"navbar-brand" %>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo03">

            
            <% if can? :create, Restoran %>
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Restaurant <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><%= link_to "List", restorans_path %></li>
                            <li><%= link_to "New", new_restoran_path %></li>
                        </ul>
                    </li>
                </ul>
            <% end %>

            <% if can? :create, Kategori %>
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Kategori <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><%= link_to "List", kategoris_path %></li>
                            <li><%= link_to "New", new_kategori_path %></li>
                        </ul>
                    </li>
                </ul>
            <% end %>

            <% if can? :create, Food %>
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Food <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><%= link_to "List", foods_path %></li>
                            <li><%= link_to "New", new_food_path %></li>
                        </ul>
                    </li>
                </ul>
            <% end %>

            <% if can? :create, Menu %>
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Menu <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><%= link_to "List", menus_path %></li>
                            <li><%= link_to "New", new_menu_path %></li>
                        </ul>
                    </li>
                </ul>
            <% end %>


            <% if can? :create, Food %>
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">User <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><%= link_to "List", persons_path %></li>
                            <li><%= link_to "New", create_member_path %></li>
                        </ul>
                    </li>
                </ul>
            <% end %>

            <% if current_user.present? %>
                <% if current_user.premium? %> 
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <%= form_tag(search_food_path, :method => "post", class: "navbar-form navbar-left") do %>
                            <div class="input-group">
                                <%= text_field_tag :search3, params[:nyari], placeholder: "Search", class: "form-control" %>
                                <%= submit_tag "Search", :class => 'btn btn-success',:name => nil %> 
                            </div>
                        <% end %>
                    </ul>
                <% end %>
            <% end %>

            <br/>

            <ul class="navbar-nav ml-auto">
                <div class="dropdown nav navbar-nav navbar-right">
                    <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Profile
                    </button>
                    <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                        <% if user_signed_in? %>
                            <li>
                                <%= link_to "Edit profile", edit_user_registration_path %>
                            </li>
                        <% else %>
                            <li>
                                <%= link_to "Sign up", new_user_registration_path %>
                            </li>
                            <li>
                                <%= link_to "Login", new_user_session_path %>
                            </li>
                        <% end %>
                        <% if user_signed_in? %>
                            <li role="separator" class="divider"></li>
                            <li>
                                <%= link_to "Logout", users_sign_out_path, method: :delete %>
                            </li>
                        <% end %>
                    </div>
                </div>
            </ul>


            
        </div>
    </div>
</nav>